document.addEventListener("DOMContentLoaded", function(event) {
  var slider = tns({
    container: ".protips",
    items: 1,
    slideBy: "page",
    autoplay: true,
    controlsPosition: "bottom"
  });
  var slider = tns({
    container: ".accolades",
    items: 1,
    slideBy: "page",
    autoplay: true,
    controlsPosition: "bottom"
  });
});
